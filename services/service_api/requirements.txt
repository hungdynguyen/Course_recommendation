# Service API Requirements (Lightweight)
# Only dependencies needed for serving API

# FastAPI & Web Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database Clients
elasticsearch==8.11.0
neo4j==5.16.0
pymysql==1.1.0

# Utilities
python-dotenv==1.0.0
requests==2.31.0

# No ML dependencies (transformers, torch, sentence-transformers)
# API only queries databases, doesn't do ML inference
